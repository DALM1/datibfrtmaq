const Textarea = () => {
  return (
    <textarea
      placeholder="@DALM1/:"
      className="globalinput"
      defaultValue="@DALM1/:"
      setinitialValue="@DALM1/:"
      //   value="@DALM1/:"
    />
  );
};

export default Textarea;
